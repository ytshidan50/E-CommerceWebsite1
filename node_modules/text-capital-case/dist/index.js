"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.capitalCase = capitalCase;
const text_no_case_1 = require("text-no-case");
const text_upper_case_first_1 = require("text-upper-case-first");
function capitalCaseTransform(input, index) {
    // Convert to lowercase first, then capitalize first letter
    return (0, text_upper_case_first_1.upperCaseFirst)(input.toLowerCase());
}
function capitalCase(input, options = {}) {
    // Handle null/undefined inputs gracefully
    if (!input)
        return "";
    return (0, text_no_case_1.noCase)(input, {
        delimiter: " ",
        transform: capitalCaseTransform,
        ...options,
    });
}
